# ─── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# ─── OS / редакторы ───────────────────────────────────────
.DS_Store
.vscode/
.idea/

# ─── логи ─────────────────────────────────────────────────
*.log

# ─── загрузки и ВЫВОДЫ прогонов (регенерируются `python -m biocode run`) ──
uploads/
results/

# ─── артефакты филогенетических инструментов (IQ-TREE / MAFFT), где бы ни лежали ──
*.treefile
*.iqtree
*.state
*.ckp.gz
*.model.gz
*.bionj
*.mldist
*.splits.nex
*.contree
*.uniqueseq.phy

# ─── артефакты MrBayes (<name>.nex.run*.*, .con.tre, .mcmc, .*stat, …) ──
# ВНИМАНИЕ: не трогает тестовую фикстуру biocode/_fixtures/sample.con.tre
*.nex.*

# ─── деревья, генерируемые anotherpipeline/build_trees.sh ──
anotherpipeline/trees/
